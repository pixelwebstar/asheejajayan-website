import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Selected Case Studies & Outcomes - Amrith Sheeja Jayan",
  description: "Explore real-world client outcomes, custom logistics boards, offline databases, and high-performance React marketing platforms.",
};

export default function ProjectLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
